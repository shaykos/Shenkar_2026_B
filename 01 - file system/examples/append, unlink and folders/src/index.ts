import {
    getRandomNumber,
    readLocalFile, writeLocalFile, appendLocalFile, deleteLocalFile,
    createFolder, deleteFolder
} from "./functions.ts";

// await readLocalFile(1);
// let n = getRandomNumber();
// await writeLocalFile(n);
// await appendLocalFile(1);
// await deleteLocalFile(10);

// await createFolder();
await deleteFolder();